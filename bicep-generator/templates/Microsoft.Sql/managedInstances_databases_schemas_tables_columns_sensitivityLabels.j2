resource {{ name }} 'Microsoft.Sql/managedInstances/databases/schemas/tables/columns/sensitivityLabels@{{ api_version|default('2021-01-01') }}' = {
  name: '{{ name }}'
  {% if parent_id %}parent: {{ parent_id }}{% endif %}
  location: {{ location|to_bicep if location else 'resourceGroup().location' }}
  properties: {
  }
}